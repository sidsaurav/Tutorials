package main

import "fmt"

func main () {
	name := "sid"

	if name == "sid" {
		fmt.Println("name is correct")
	} else {
		fmt.Println("name is not correct")
	}

	// we can also dec variable in if
	
	if num:=5; num > 10 {
		fmt.Println("Num is greater than 10")
	} else {
		fmt.Println("Num is less than 10")
	}
}
